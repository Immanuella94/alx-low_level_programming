#include "holberton.h"

/**
 * _sqrt_recursion - returns sqare root of a number
 * and new line
 *
 * @n: argument
 *
 * Return: Always 0
 */

int _sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	if (i != 0)
	{
		i = i - 1;
		return (n * _sqrt_recursion(n
