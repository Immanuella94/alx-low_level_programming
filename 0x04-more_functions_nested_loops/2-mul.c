#include <stdio.h>
#include "holberton.h"

/**
 * mul - function that multiplies two intagers
 * @a: intager to be multiplied
 * @b: intager to be multiplied
 * Return: result of multiplication of two integers
 */

int mul(int a, int b)
{
	mul = a * b;

	return (mul);
}
